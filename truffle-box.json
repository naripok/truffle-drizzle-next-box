{
  "ignore": [],
  "commands": {
    "Compile contracts": "truffle compile",
    "Migrate contracts": "truffle migrate",
    "Test contracts": "truffle test"
  },
  "hooks": {
    "post-unpack": "truffle compile && cd web-app && yarn install && yarn link-contracts"
  }
}
